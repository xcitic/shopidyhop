export interface Product {
  id: string;
  title: string;
  price: string;
  category: string,
  description: string;
  image: string;
}

export interface ProductCategory {
  id: string;
  name: string;
}